# Environment Variables
# Copy this file to .env.local and fill in your values.
# Arka plan bildirimleri (iOS/Chrome) için tam kurulum: docs/PUSH_SETUP.md

# Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: If you want to use a different API endpoint
# NEXT_PUBLIC_ALADHAN_API_URL=https://api.aladhan.com

# --- Web Push (iOS 16.4+ PWA & Chrome background notifications) ---
# Generate VAPID keys: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Upstash Redis (free tier: https://upstash.com) - stores push subscriptions
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Vercel Cron auth (set in Vercel dashboard; cron calls /api/cron/push-reminders every minute)
CRON_SECRET=
